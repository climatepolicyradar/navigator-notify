[tool.poetry]
name = "navigator-notify"
version = "0.1.0"
description = ""
authors = ["olaughter <fred@climatepolicyradar.org>"]
readme = "README.md"
packages = [{include = "navigator_notify"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
